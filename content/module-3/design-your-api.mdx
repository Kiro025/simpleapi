# Designing Your API

The worst time to think about your API's structure is while you're writing code. Five minutes spent designing before you write a single line saves hours of refactoring later.

## Step 1: Identify Your Resources

A resource is any **noun** your API manages. For a task manager:

- **Tasks** — the core resource
- **Users** — who owns tasks (Phase 2)
- **Tags** — categories for tasks (Phase 2)

Start with one resource and get it right. You're building the **Tasks API**.

## Step 2: Define the URL Structure

Each resource gets a collection URL and an item URL:

<div className="my-5 rounded-xl border border-border overflow-hidden">
  <div className="grid grid-cols-[1fr_2fr] bg-muted/50 border-b border-border">
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">URL</div>
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Identifies</div>
  </div>
  <div className="grid grid-cols-[1fr_2fr] border-b border-border/50 items-center">
    <div className="px-4 py-2.5 text-[14px]"><code>/tasks</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">The entire collection of tasks</div>
  </div>
  <div className="grid grid-cols-[1fr_2fr] items-center">
    <div className="px-4 py-2.5 text-[14px]"><code>/tasks/:id</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">A specific task</div>
  </div>
</div>

Rules:
- Plural nouns: `/tasks`, not `/task`
- No verbs: `/tasks`, not `/getTasks`
- Nested resources use nested paths: `/tasks/:id/comments`

## Step 3: Map Methods to Operations

Now assign an HTTP method to each operation:

<div className="my-5 rounded-xl border border-border overflow-x-auto">
  <div className="min-w-[640px]">
    <div className="grid grid-cols-[100px_120px_1fr_1fr_1fr] bg-muted/50 border-b border-border">
      <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Method</div>
      <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">URL</div>
      <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">What it does</div>
      <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Request body</div>
      <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Response</div>
    </div>
    <div className="grid grid-cols-[100px_120px_1fr_1fr_1fr] border-b border-border/50 items-center">
      <div className="px-4 py-2.5"><MethodBadge method="GET" /></div>
      <div className="px-4 py-2.5 text-[14px]"><code>/tasks</code></div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">List all tasks</div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">—</div>
      <div className="px-4 py-2.5 text-[14px]">200 + <code>{"{ data: Task[] }"}</code></div>
    </div>
    <div className="grid grid-cols-[100px_120px_1fr_1fr_1fr] border-b border-border/50 items-center">
      <div className="px-4 py-2.5"><MethodBadge method="POST" /></div>
      <div className="px-4 py-2.5 text-[14px]"><code>/tasks</code></div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Create a task</div>
      <div className="px-4 py-2.5 text-[14px]"><code>{"{ title, status?, priority? }"}</code></div>
      <div className="px-4 py-2.5 text-[14px]">201 + <code>{"{ data: Task }"}</code></div>
    </div>
    <div className="grid grid-cols-[100px_120px_1fr_1fr_1fr] border-b border-border/50 items-center">
      <div className="px-4 py-2.5"><MethodBadge method="GET" /></div>
      <div className="px-4 py-2.5 text-[14px]"><code>/tasks/:id</code></div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Get one task</div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">—</div>
      <div className="px-4 py-2.5 text-[14px]">200 + <code>{"{ data: Task }"}</code> or 404</div>
    </div>
    <div className="grid grid-cols-[100px_120px_1fr_1fr_1fr] border-b border-border/50 items-center">
      <div className="px-4 py-2.5"><MethodBadge method="PUT" /></div>
      <div className="px-4 py-2.5 text-[14px]"><code>/tasks/:id</code></div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Replace a task</div>
      <div className="px-4 py-2.5 text-[14px]"><code>{"{ title, status, priority }"}</code></div>
      <div className="px-4 py-2.5 text-[14px]">200 + <code>{"{ data: Task }"}</code> or 404</div>
    </div>
    <div className="grid grid-cols-[100px_120px_1fr_1fr_1fr] border-b border-border/50 items-center">
      <div className="px-4 py-2.5"><MethodBadge method="PATCH" /></div>
      <div className="px-4 py-2.5 text-[14px]"><code>/tasks/:id</code></div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Partial update</div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">any subset of fields</div>
      <div className="px-4 py-2.5 text-[14px]">200 + <code>{"{ data: Task }"}</code> or 404</div>
    </div>
    <div className="grid grid-cols-[100px_120px_1fr_1fr_1fr] items-center">
      <div className="px-4 py-2.5"><MethodBadge method="DELETE" /></div>
      <div className="px-4 py-2.5 text-[14px]"><code>/tasks/:id</code></div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Delete a task</div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">—</div>
      <div className="px-4 py-2.5 text-foreground/85 text-[14px]">204 or 404</div>
    </div>
  </div>
</div>

## Step 4: Define Your Data Model

The shape of a Task object:

```js
{
  id: 1,                        // number, auto-assigned
  title: "Write the API",       // string, required, 1-200 chars
  status: "todo",               // "todo" | "in-progress" | "done"
  priority: "high",             // "low" | "medium" | "high"
  createdAt: "2026-04-20T..."   // ISO 8601, server-assigned
}
```

Fields that are **server-assigned** (id, createdAt) must never be accepted from the client in POST/PUT/PATCH.

## Step 5: Define Validation Rules

<div className="my-5 rounded-xl border border-border overflow-hidden">
  <div className="grid grid-cols-[120px_1fr] bg-muted/50 border-b border-border">
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Field</div>
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Rule</div>
  </div>
  <div className="grid grid-cols-[120px_1fr] border-b border-border/50 items-start">
    <div className="px-4 py-2.5 text-[14px]"><code>title</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Required on POST and PUT. String. 1–200 characters.</div>
  </div>
  <div className="grid grid-cols-[120px_1fr] border-b border-border/50 items-start">
    <div className="px-4 py-2.5 text-[14px]"><code>status</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Optional. Must be <code>"todo"</code>, <code>"in-progress"</code>, or <code>"done"</code>. Defaults to <code>"todo"</code>.</div>
  </div>
  <div className="grid grid-cols-[120px_1fr] items-start">
    <div className="px-4 py-2.5 text-[14px]"><code>priority</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Optional. Must be <code>"low"</code>, <code>"medium"</code>, or <code>"high"</code>. Defaults to <code>"medium"</code>.</div>
  </div>
</div>

## Step 6: Define Error Responses

<div className="my-5 rounded-xl border border-border overflow-hidden">
  <div className="grid grid-cols-[1fr_80px_2fr] bg-muted/50 border-b border-border">
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Scenario</div>
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Status</div>
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Body</div>
  </div>
  <div className="grid grid-cols-[1fr_80px_2fr] border-b border-border/50 items-start">
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Task not found</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">404</div>
    <div className="px-4 py-2.5 text-[14px]"><code>{"{ error: \"Task with id 5 not found\" }"}</code></div>
  </div>
  <div className="grid grid-cols-[1fr_80px_2fr] border-b border-border/50 items-start">
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Missing title</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">422</div>
    <div className="px-4 py-2.5 text-[14px]"><code>{"{ error: \"Validation failed\", message: \"title is required\", field: \"title\" }"}</code></div>
  </div>
  <div className="grid grid-cols-[1fr_80px_2fr] border-b border-border/50 items-start">
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Invalid status</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">422</div>
    <div className="px-4 py-2.5 text-[14px]"><code>{"{ error: \"Validation failed\", message: \"status must be todo, in-progress, or done\" }"}</code></div>
  </div>
  <div className="grid grid-cols-[1fr_80px_2fr] items-start">
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Bad JSON body</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">400</div>
    <div className="px-4 py-2.5 text-[14px]"><code>{"{ error: \"Invalid JSON body\" }"}</code></div>
  </div>
</div>

## Step 7: Define Query Parameter Support

For the list endpoint (`GET /tasks`):

<div className="my-5 rounded-xl border border-border overflow-hidden">
  <div className="grid grid-cols-[120px_1fr_2fr] bg-muted/50 border-b border-border">
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Param</div>
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Type</div>
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Effect</div>
  </div>
  <div className="grid grid-cols-[120px_1fr_2fr] border-b border-border/50 items-center">
    <div className="px-4 py-2.5 text-[14px]"><code>status</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">string</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Filter by status</div>
  </div>
  <div className="grid grid-cols-[120px_1fr_2fr] border-b border-border/50 items-center">
    <div className="px-4 py-2.5 text-[14px]"><code>priority</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">string</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Filter by priority</div>
  </div>
  <div className="grid grid-cols-[120px_1fr_2fr] border-b border-border/50 items-center">
    <div className="px-4 py-2.5 text-[14px]"><code>sort</code></div>
    <div className="px-4 py-2.5 text-[14px]"><code>createdAt</code> | <code>priority</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Sort field</div>
  </div>
  <div className="grid grid-cols-[120px_1fr_2fr] border-b border-border/50 items-center">
    <div className="px-4 py-2.5 text-[14px]"><code>order</code></div>
    <div className="px-4 py-2.5 text-[14px]"><code>asc</code> | <code>desc</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Sort direction (default: <code>asc</code>)</div>
  </div>
  <div className="grid grid-cols-[120px_1fr_2fr] border-b border-border/50 items-center">
    <div className="px-4 py-2.5 text-[14px]"><code>page</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">number</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Page number (default: <code>1</code>)</div>
  </div>
  <div className="grid grid-cols-[120px_1fr_2fr] items-center">
    <div className="px-4 py-2.5 text-[14px]"><code>limit</code></div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">number</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Items per page (default: <code>10</code>, max: <code>100</code>)</div>
  </div>
</div>

## The Completed Spec

This is your blueprint. Before writing any code, you can hand this spec to another developer (or a frontend team) and they know exactly what to expect:

```text filename="tasks-api-spec.txt"
Tasks API v1

Base URL: /api/tasks

Resources
─────────
Task {
  id:        number   (server-assigned)
  title:     string   (required, 1-200 chars)
  status:    "todo" | "in-progress" | "done"  (default: "todo")
  priority:  "low" | "medium" | "high"        (default: "medium")
  createdAt: ISO 8601 string  (server-assigned)
}

Endpoints
─────────
GET    /tasks          → 200 { data: Task[], meta: { total, page, limit } }
POST   /tasks          → 201 { data: Task }      body: { title, status?, priority? }
GET    /tasks/:id      → 200 { data: Task }      | 404
PUT    /tasks/:id      → 200 { data: Task }      body: { title, status, priority }
PATCH  /tasks/:id      → 200 { data: Task }      body: partial Task fields
DELETE /tasks/:id      → 204 (empty)             | 404

Errors
──────
400  Invalid JSON
404  Resource not found
422  Validation failed  { error, message, field? }
```

<Callout variant="success" title="Design-first pays off">
You now have a contract. Your implementation will verify against this spec. If your POST returns 200 instead of 201, or your 404 message format is inconsistent — the spec catches it. Good API design is consistent, predictable, and self-documenting.
</Callout>

Try the live Tasks API — see how closely it matches the spec above:

<RequestPlayground
  defaultMethod="GET"
  defaultUrl="/api/playground/tasks"
  title="GET /tasks — list all"
  description="Notice the response envelope: { data, meta, _links }. Compare the Task shape to the spec above."
/>

<RequestPlayground
  defaultMethod="POST"
  defaultUrl="/api/playground/tasks"
  defaultBody={`{
  "title": "Design the API spec",
  "priority": "high"
}`}
  title="POST /tasks — create"
  description="id and createdAt are server-assigned — you don't send them. Notice the 201 status."
/>

<QuizCard
  question="Why should URLs use plural nouns like /tasks instead of /task or /getTask?"
  choices={["It's just a convention with no practical impact", "Plural nouns make it clear the URL refers to a resource type (collection), and verbs in URLs make the API harder to cache and understand", "Singular nouns cause routing conflicts in Express", "REST requires plural URLs as part of the HTTP specification"]}
  correct={1}
  explanation="/tasks identifies the 'tasks' resource — a collection. Verbs like /getTasks or /createTask mix the resource and operation, breaking the Uniform Interface constraint. HTTP methods already carry the verb (GET, POST, DELETE), so URLs should be pure nouns."
/>

<QuizCard
  question="A client sends `{ id: 999, title: 'New task' }` to POST /tasks. Your server should:"
  choices={["Accept the id and create the task with id=999", "Return 400 because id is not allowed", "Ignore the id field and assign one server-side", "Return 422 because id is not in the schema"]}
  correct={2}
  explanation="Server-assigned fields (id, createdAt) should be silently ignored or explicitly stripped from client input — never trusted. Your Zod schema shouldn't include them, so they simply won't appear in the validated output. The server always generates its own id."
/>
