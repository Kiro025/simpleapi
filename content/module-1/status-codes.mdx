# HTTP Status Codes

Every HTTP response includes a three-digit status code that tells the client what happened. You've already seen some — now let's understand them systematically.

## The Five Families

<div className="my-5 rounded-xl border border-border overflow-hidden">
  <div className="grid grid-cols-[70px_1fr_2fr] bg-muted/50 border-b border-border">
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Range</div>
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Category</div>
    <div className="px-4 py-2.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Meaning</div>
  </div>
  <div className="grid grid-cols-[70px_1fr_2fr] border-b border-border/50 items-center">
    <div className="px-4 py-2.5 text-[14px] font-mono font-semibold text-foreground/70">1xx</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Informational</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Request received, processing continues</div>
  </div>
  <div className="grid grid-cols-[70px_1fr_2fr] border-b border-border/50 items-center">
    <div className="px-4 py-2.5 text-[14px] font-mono font-semibold text-green-600 dark:text-green-400">2xx</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Success</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Request received, understood, and accepted</div>
  </div>
  <div className="grid grid-cols-[70px_1fr_2fr] border-b border-border/50 items-center">
    <div className="px-4 py-2.5 text-[14px] font-mono font-semibold text-blue-500 dark:text-blue-400">3xx</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Redirection</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Further action needed</div>
  </div>
  <div className="grid grid-cols-[70px_1fr_2fr] border-b border-border/50 items-center">
    <div className="px-4 py-2.5 text-[14px] font-mono font-semibold text-yellow-600 dark:text-yellow-400">4xx</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Client Error</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">The client made a mistake</div>
  </div>
  <div className="grid grid-cols-[70px_1fr_2fr] items-center">
    <div className="px-4 py-2.5 text-[14px] font-mono font-semibold text-destructive">5xx</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">Server Error</div>
    <div className="px-4 py-2.5 text-foreground/85 text-[14px]">The server failed to fulfill a valid request</div>
  </div>
</div>

## 2xx — Success

- **200 OK** — Standard success. Returned by most GET and PUT requests.
- **201 Created** — Resource was created. Use this after a successful POST.
- **204 No Content** — Success, but nothing to return. Common for DELETE.

<RequestPlayground
  defaultMethod="GET"
  defaultUrl="/api/playground/users"
  title="200 OK example"
  description="A successful GET returns 200 with the data in the body."
/>

## 4xx — Client Errors

<Callout variant="warning" title="Client errors are your fault (as the caller)">
4xx codes mean the request itself was wrong. A good API always includes a clear error message in the response body — not just the code.
</Callout>

- **400 Bad Request** — The server couldn't understand the request (bad JSON, missing required fields)
- **401 Unauthorized** — You need to authenticate first
- **403 Forbidden** — Authenticated but not allowed to do this
- **404 Not Found** — The resource doesn't exist at this URL

Try triggering a **404**:

<RequestPlayground
  defaultMethod="GET"
  defaultUrl="/api/playground/users/999"
  title="404 Not Found"
  description="User 999 doesn't exist. Notice the error message in the response body — that's good API design."
/>

Try triggering a **400**:

<RequestPlayground
  defaultMethod="POST"
  defaultUrl="/api/playground/users"
  defaultBody={`{"role": "admin"}`}
  title="400 Bad Request"
  description="Missing the required 'name' and 'email' fields."
/>

## 5xx — Server Errors

- **500 Internal Server Error** — Something unexpected failed on the server
- **502 Bad Gateway** — The server got an invalid response from an upstream service
- **503 Service Unavailable** — Server is temporarily unavailable (overloaded or down for maintenance)

<Callout variant="info" title="5xx vs 4xx">
5xx errors are the server's fault. 4xx errors are the client's fault. This distinction matters for debugging: a 400 means you sent something wrong; a 500 means something broke on the server side.
</Callout>

## 422 — Unprocessable Entity

There's an important distinction between **400 Bad Request** and **422 Unprocessable Entity**:

- **400 Bad Request** — the server couldn't parse the request (malformed JSON, wrong content-type)
- **422 Unprocessable Entity** — the JSON was valid, but the data itself failed validation (missing required field, wrong value type)

```json
// 400 — can't even parse this
POST /tasks  body: "title: missing quotes"

// 422 — valid JSON, but semantically wrong
POST /tasks  body: { "status": "pending" }
// → 422 { "error": "Validation failed", "message": "'title' is required" }
```

Most modern APIs use 422 for field-level validation failures instead of 400.

## 409 — Conflict

**409 Conflict** means the request can't be completed because it conflicts with the current state of the resource. Common cases:

- Creating a user with an email that already exists
- Trying to update a resource that's been modified by another user (optimistic locking)

```json
POST /users  { "email": "alice@example.com" }
→ 409 { "error": "A user with this email already exists" }
```

## Consistent Error Response Shape

A status code tells the client *what* happened. The response body should tell them *why* and *where*.

```json
// ✅ Good — specific, actionable
{
  "error": "Validation failed",
  "message": "'title' must not be empty",
  "field": "title"
}

// ❌ Bad — vague, unhelpful
{
  "message": "error"
}
```

The `field` property is especially useful for form validation — the client can highlight the specific input that failed.

## Status Codes as a Communication Protocol

The status code is the first thing a client reads. It answers: "did it work?" before the client even parses the body.

A well-designed API uses status codes correctly so clients can make decisions without reading the body — for example, retrying on 503 but not on 400.

Try triggering a **422** (validation failure):

<RequestPlayground
  defaultMethod="POST"
  defaultUrl="/api/playground/tasks"
  defaultBody={`{
  "status": "todo",
  "priority": "high"
}`}
  title="422 — missing required field"
  description="The 'title' field is required. Notice the response includes 'error', 'message', and 'field' — a consistent error shape."
/>

Try triggering a **500** (simulated server error):

<RequestPlayground
  defaultMethod="GET"
  defaultUrl="/api/playground/error"
  title="500 Internal Server Error"
  description="This endpoint always returns a 500 to demonstrate what a server-side failure looks like."
/>

<QuizCard
  question="Your API call returns 403. What does this tell you?"
  choices={["The resource doesn't exist", "The server crashed", "You're authenticated but not allowed to perform this action", "You need to log in first"]}
  correct={2}
  explanation="403 Forbidden means the server knows who you are (you're authenticated) but you don't have permission for this action. 401 Unauthorized is what you get when you haven't authenticated at all."
/>

<QuizCard
  question="Which status code should a successful POST that creates a new resource return?"
  choices={["200 OK", "201 Created", "204 No Content", "202 Accepted"]}
  correct={1}
  explanation="201 Created is the correct code after a POST that creates a new resource. 200 OK is for successful reads (GET) and updates (PUT/PATCH). 204 No Content is for operations that succeed but have nothing to return, like DELETE."
/>
